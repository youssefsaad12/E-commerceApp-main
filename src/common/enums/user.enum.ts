export enum ProviderEnum {
  system = "system",
  google = "google",
}

export enum GenderEnum {
  male = "male",
  female = "female",
}

export enum RoleEnum {
  user = "user",
  admin = "admin",
  superAdmin = "superAdmin",
}